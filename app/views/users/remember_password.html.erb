<p>Select your session and enter your username (not your email address).
   If you don't remember your username you must contact <a href="mailto:support@legsim.org"> support@legsim.org </a> </p>

<%= form_tag request_password_reset_users_path do -%>

  <p><%= label_tag 'course_id' %><br/>
  <%= select_tag 'course_id', options_for_select( @courses.collect {|c| [ c.title, c.id ] } ) %></p>

  <p><%= label_tag 'Username' %><br />
  <%= text_field_tag 'login', @login %></p>

  <%= submit_tag "Request Password Reset" %>

<% end %>
